<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="foodexpiry">


    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UtilityApp">
        <activity android:name="com.studio.foodexpiry.SplashActivity"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".ToDoListActivity"
            android:label="To Do List"/>
        <activity android:name="com.studio.foodexpiry.MainActivity" />
        <activity android:name=".PlanMyDayActivity"
            android:label="Plan My Day"/>
        <activity android:name=".SignupFrontend.LoginActivity" />
        <activity android:name=".SignupFrontend.RegisterActivity" />
        <activity android:name="com.studio.foodexpiry.FoodList" />
        <activity android:name="com.studio.foodexpiry.DetailsActivity" />
        <activity android:name="com.studio.foodexpiry.LoginActivity" />
        <activity android:name="com.studio.foodexpiry.RegisterActivity" />
    </application>

</manifest>